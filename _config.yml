# The Site Configuration

theme: jekyll-theme-chirpy

lang: en

# Change to your timezone (optional, e.g., Asia/Jerusalem or UTC)
timezone: UTC

# jekyll-seo-tag settings
title: "Moataz's Notes"
tagline: "Documenting my research journey"
description: >-
  Documenting my research journey: deep learning, computer vision, self-supervised learning paper summaries, implementations, and experiments.

# Your GitHub Pages URL (no trailing slash)
url: "https://moatazhamza194.github.io"

github:
  username: moatazhamza194

social:
  name: Moataz Hamza
  email: moatazhamza194@gmail.com
  links:
    - https://github.com/moatazhamza194

# Prefer color scheme setting
# Options: light | dark | empty (follows browser settings with a toggle)
theme_mode:

# Sidebar avatar image (local path or external URL)
avatar: "https://github.com/moatazhamza194.png"

# Global switch for Table of Contents in posts
toc: true

actions:
  edit_post:
    enabled: false

# PWA settings
pwa:
  enabled: true
  cache:
    enabled: true

paginate: 10

baseurl: ""

# ------------ System Defaults (Do Not Change) ------------------

kramdown:
  footnote_backlink: "&#8617;&#xfe0e;"
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    css_class: highlight
    span:
      line_numbers: false
    block:
      line_numbers: true
      start_line: 1

collections:
  tabs:
    output: true
    sort_by: order

defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: post
      comments: true
      toc: true
      permalink: /posts/:title/
  - scope:
      path: _drafts
    values:
      comments: false
  - scope:
      path: ""
      type: tabs
    values:
      layout: page
      permalink: /:title/

sass:
  style: compressed

compress_html:
  clippings: all
  comments: all
  endings: all
  profile: false
  blanklines: false
  ignore:
    envs: [development]

exclude:
  - "*.gem"
  - "*.gemspec"
  - docs
  - tools
  - README.md
  - LICENSE
  - purgecss.js
  - "*.config.js"
  - "package*.json"

jekyll-archives:
  enabled: [categories, tags]
  layouts:
    category: category
    tag: tag
  permalinks:
    tag: /tags/:name/
    category: /categories/:name/
